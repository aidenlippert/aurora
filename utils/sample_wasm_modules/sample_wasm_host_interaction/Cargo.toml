[package]
name = "sample_wasm_host_interaction"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]
test = false

[dependencies]
wee_alloc = "0.4.5" # ADDED for heap allocation in no_std Wasm