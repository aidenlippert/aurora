[package]
name = "sample_wasm_host_interaction"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
# No special dependencies needed for this simple example

# Add panic = "abort" for release builds to satisfy no_std Wasm requirements
[profile.release]
panic = "abort"
